export function renderDocument(document) { const content = document.content; return `<!doctype html><html><head><style>${document.style}</style></head><body><h1>${content.title}</h1><main>${content.body}</main></body></html>`; }
