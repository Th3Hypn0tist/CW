export function renderDocument(document) { const d = document ?? {}; if (d.error) return `<!doctype html><html><body><h1>Not Found</h1><p>${d.error.code}</p></body></html>`; const content = d.content ?? {}; const style = d.style ?? {}; return `<!doctype html><html><head><style>${style.css ?? ''}</style></head><body><h1>${content.title ?? ''}</h1><main>${content.body ?? ''}</main></body></html>`; }
